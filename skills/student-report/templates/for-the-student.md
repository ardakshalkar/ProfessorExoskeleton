<!--
  This one is handed to the student, so the register changes and the shape with
  it. Three rules the professor's version does not have:

  - It must not read as a verdict. "Not yet demonstrated" is a statement about
    the record and it lands, on the person it is about, as a judgement.
  - A blank says in words that nothing has measured it yet. A student reading a
    gap as a failure is the injustice this whole model exists to avoid.
  - Internal identifiers stay out of the prose. Keep them in the last section so
    the professor can still check it.

  The professor reads it before the student does — always, and say so when you
  hand it over. Written outside the workspace, pseudonym in the filename.

  Fill every {{ slot }}. Delete this comment.
-->

# {{ COURSE_ID }} {{ course title }} — where you are

*{{ term }}, as of {{ YYYY-MM-DD }}. This is what the coursework so far has
shown. It is not a grade, and it is not final.*

## What you have shown you can do

- **{{ what the outcome says, in plain words }}** — from {{ which pieces of work,
  named the way the student knows them }}. {{ What was good about it. }}

## What has not been looked at yet

<!-- Never a list of failures. These are gaps in what the course has measured. -->

- **{{ topic in plain words }}** — nothing so far has assessed this, so there is
  nothing to say about it yet. It comes up in {{ where }}.

## What to work on

<!-- Where the evidence is weak. Say what the work showed, then what to do — and
     name what it rests on, because that is usually where the fix is. -->

- **{{ topic in plain words }}.** {{ What the work showed, concretely. }} This
  builds on {{ the earlier topic }}, so {{ what to go back to first }}.

## What to do next

1. {{ A specific, finishable step. }}
2. {{ A second, if there is one. }}

{{ Where to get help, and by when it is worth asking. }}

---

*Prepared from your recorded coursework and reviewed by
{{ the professor's name or role }} before you received it.*

<!-- For the professor, not for the student — delete before handing over, or
     keep if the report is being filed rather than sent:
     evidence {{ EVID-… }}; from `ainar student {{ STUDENT-XXXXXX }}
     --course-version {{ COURSE_VERSION_ID }}` on {{ YYYY-MM-DD }}. -->
