Originally unpacked by `python -m exo setup` from ProfessorSkills, on
2026-09-04.

That link is cut. This directory is source: edit it here. Nothing writes into
it from outside this repository any more, and there is no setup step to re-run.

One thing to know about this copy: it was taken with a plain recursive copy
rather than through `exo setup`, which omits node_modules. So it carries the
63 MB of dependencies that were installed in the checkout it came from — which
is why `pres` works here without the separate npm install its README asks for.

See PROVENANCE.md at the repository root for where every tree came from.
