<!--
  STUDENTS FORK THIS REPOSITORY.

  Everything in the repository is read by the class, and git keeps whatever was
  ever committed. So: no answer key, no correct-option marker, no marking
  guidance, no misconception notes, no solution or reference implementation — not
  in this file, not in a comment, not in a test fixture, not in a `solutions/`
  directory "for later". Re-read every file in the scaffold against that list
  before reporting.

  The repository itself is not yours to create. Build the files, print the `gh`
  commands, and stop.

  Fill every {{ slot }}. Delete this comment.
-->

# {{ assessment title }} — starter

**{{ COURSE_ID }} {{ course title }} · {{ term }}**

Due {{ YYYY-MM-DD, HH:MM +05:00 }}. The full brief is
{{ where the brief lives — the course page, the LMS, or the file beside this }}.

## What you are building

{{ One paragraph. Enough to start; the brief carries the rest. }}

## Getting set up

```
{{ the commands, exactly — clone, install, and the one that proves it works }}
```

If {{ the proving command }} does not {{ the expected result }}, {{ what to do }}.

## What is here

```
{{ path }}      {{ what it is, and whether students edit it }}
{{ path }}      {{ what it is }}
```

## What you write

- `{{ path }}` — {{ what goes there }}
- `{{ path }}` — {{ what goes there }}

Do not change {{ the files that must stay as they are, and why }}.

## Checking your work

{{ Only if there are tests students may run. Say what passing means and — this
part matters — what it does not: passing tests students can run is not the same
as meeting the rubric, and a student who believes otherwise has been misled by
this file. If there are no such tests, delete this section. }}

## Handing in

{{ What, where, in what form, by when. }}

## How it is marked

The criteria and their level descriptions are in the brief. {{ Link or name it. }}
Read them before you start — they are what the work is judged against.
