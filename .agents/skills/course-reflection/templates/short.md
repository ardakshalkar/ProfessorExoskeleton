<!--
  The one-page version, for the week when the alternative is not writing one at
  all. It drops the discussion, not the structure: the same five questions,
  answered in a line each.

  Three things are not dropped, because a note without them is a note nobody can
  use next year: the separation between what the evidence showed and what you
  noticed, the changes that are claims rather than fixes, and the check to run
  next year.

  Fill every {{ slot }}. Delete this comment.
-->

# {{ COURSE_ID }}, {{ term }} — reflection (short)

*Written {{ YYYY-MM-DD }}. {{ n }} students, {{ n }} assessments.*

**The evidence.** {{ Two or three sentences, with the figures and where they came
from. }}

**What I noticed, unevidenced.** {{ One or two sentences. Kept separate on
purpose — this is a hypothesis, not a finding. }}

**Changing next term.**

- Delivery: {{ change }}
- Instrument: {{ change }}
- Needs a decision (a claim, not a fix): {{ the question }}

**Not changing, on purpose.** {{ What you considered and are keeping, in a
line. }}

**To check next year.** {{ The question, phrased so next year's evidence can
answer it. }}
