---
marp: true
title: "{{ session title }}"
module: "{{ MODULE-ID }}"
course_run: "{{ COURSE_VERSION_ID }}"
outcomes: ["{{ LO-ID }}"]
concepts: ["{{ CONCEPT-ID }}"]
generated_by: make-materials-skill
---

<!--
  Fill every {{ slot }} and delete every slide you do not use, including this
  comment. Slide order and count must match the `presentation_plan` on the
  Document draft, or the renderer stops and says where.

  Every concept named here belongs to the module. If the session needs one the
  module does not have, that is a claim, and it is the professor's — stop and
  say which, rather than adding it.
-->

# {{ session title }}

{{ MODULE-ID }} · {{ COURSE_ID }} {{ term }} · {{ date }}

---

## Where we are

<!-- Open by placing the session in the sequence. Both halves are read from the
     module before this one; neither is a summary of what you hope they know. -->

- Last time: {{ what the previous module covered }}
- Today: {{ what this session adds }}
- This builds on {{ CONCEPT-ID }}, which {{ where it was taught }}

---

## {{ the known weakness, if there is one }}

<!-- If item responses or a signal show a shared misconception, correct it here,
     first, before the new content — and say which item revealed it. If there is
     no recorded misconception, delete this slide rather than inventing one. -->

- What the responses to {{ ITEM-ID }} showed: {{ what the class converged on }}
- Why it is wrong: {{ the correction }}
- What to hold on to instead: {{ the rule that replaces it }}

---

## {{ concept title }}

<!-- One concept per slide where the structure matters, named by identifier so
     the material and the model stay connected. -->

{{ the explanation }}

`{{ CONCEPT-ID }}`

---

## {{ figure title }}

<!-- The picture is generated by something you also commit — an SVG, a manim
     scene, a chart from an ainar command — never pasted in alone. Alt text is
     not optional; slides are read by people who cannot see them. -->

![{{ alt text: what the figure shows, in a sentence }}]({{ deck-name }}-fig-01-{{ what }}.svg)

---

## {{ worked example }}

<!-- An example you construct yourself is fine and is labelled as one. A
     statistic, dataset or quotation you cannot cite is not. -->

{{ the example, worked through }}

---

## Check yourself

<!-- One per new concept, phrased as a question a student answers rather than a
     topic they nod at. -->

1. {{ question about CONCEPT-ID }}
2. {{ question about CONCEPT-ID }}

---

## Next

- {{ what the next session does with this }}
- Before then: {{ the reading or task, or delete this line }}
