<!--
  STUDENTS READ THIS FILE.

  Before it is finished, re-read it against the skill's §6: no `answer_key`, no
  correct-option marker, no `marking_guidance`, no `indicates_misconception_of`
  or its note, no solution or reference implementation, no student's name. That
  check cannot be undone after the fact — a file that briefly held the answer key
  held it for whoever was looking.

  The weight below comes from the professor. If they have not stated one, it
  stays TODO and you ask; a weight is a claim about how students are judged, and
  it is not yours to choose. Fill every {{ slot }}. Delete this comment.
-->

# {{ assessment title }}

**{{ COURSE_ID }} {{ course title }} · {{ term }}**

| | |
| --- | --- |
| Due | {{ YYYY-MM-DD, HH:MM +05:00 }} |
| Weight | {{ the recorded weight, or "TODO — not yet decided" }} |
| Submit | {{ where and in what form }} |
| Work | {{ individually / in pairs / in groups of n }} |

## What this assesses

<!-- Outcomes as recorded on the assessment. Copy the wording; do not paraphrase
     it into something that reads better, because the wording is the claim. -->

- `{{ LO-ID }}` — {{ the outcome, verbatim }}

## The task

{{ What to do, in enough detail that a student can start without asking. Where
the task builds on something taught, name the concept by identifier. }}

## What to hand in

1. {{ deliverable }} — {{ format, length or size limit }}
2. {{ deliverable }} — {{ format }}

{{ What happens if it is late, if that is recorded on the assessment; delete this
line rather than inventing a policy. }}

## How it is marked

<!-- The rubric goes in the brief. A criterion a student cannot read before
     starting is a criterion they cannot aim at, and the level descriptions are
     the most useful thing in this document. -->

### {{ CRIT-ID }} — {{ criterion title }} ({{ n }} marks)

Measures `{{ LO-ID }}`.

| Level | What it looks like |
| --- | --- |
| {{ level name }} | {{ description, verbatim from the criterion }} |
| {{ level name }} | {{ description }} |
| {{ level name }} | {{ description }} |

### {{ CRIT-ID }} — {{ criterion title }} ({{ n }} marks)

Measures `{{ LO-ID }}`.

| Level | What it looks like |
| --- | --- |
| {{ level name }} | {{ description }} |

**Total: {{ sum of the criterion maxima }} marks.**

## What you may use

{{ Permitted tools, materials, collaboration, AI assistance — as the course
records it. If nothing is recorded, say that it has not been stated and that
students should ask, rather than writing a policy nobody set. }}

## Questions

{{ Where to ask, and by when. }}

---

*Drafted from {{ ASSESSMENT-ID }} on {{ YYYY-MM-DD }}. Not in force until the
professor approves it.*
